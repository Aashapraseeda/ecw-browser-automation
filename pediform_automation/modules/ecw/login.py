"""
modules/ecw/login.py
--------------------
ECW login automation (placeholder — implement when ECW upload phase is enabled).
"""

from utils.logger import get_logger
logger = get_logger(__name__)


async def login(page, ecw_url: str, username: str, password: str) -> None:
    logger.warning("[ECW] login() not yet implemented — skipping.")
